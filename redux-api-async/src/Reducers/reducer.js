
const initialState = {
    greeting: 'Welcome. This is the initial state.'
}

const reducer = (state = initialState, action) => {
    switch (action.type) {
        default:
            return state
    }
}

export default reducer